<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Delivery" parent="Theme.Material3.DayNight.NoActionBar">

        <item name="toolbarStyle">@style/Widget.Delivery.Toolbar</item>
        <item name="toolbarWithArrowButtonStyle">@style/Widget.Delivery.Toolbar.WithArrow</item>
        <item name="toolbarWithCloseButtonStyle">@style/Widget.Delivery.Toolbar.WithClose</item>

        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Delivery" parent="Base.Theme.Delivery" />

    <style name="AppTheme.AppBarOverlay" parent="Theme.Delivery">
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/black</item>
        <item name="android:background">@null</item>
    </style>
</resources>